{
  "name": "@coti-io/sablier-payroll-port",
  "version": "0.1.0",
  "description": "Sablier Merkle Instant payroll user-story tests (Phase 1 reference harness)",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "npm run compile && SABLIER_PAYROLL_TESTS=1 hardhat test test/runner.ts",
    "test:sablier-payroll": "npm test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.1",
    "@types/node": "^22.19.5",
    "dotenv": "^17.4.2",
    "hardhat": "^3.1.3",
    "typescript": "~5.8.0",
    "viem": "^2.44.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
